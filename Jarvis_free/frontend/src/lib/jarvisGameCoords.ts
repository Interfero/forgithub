/** Логическая 2D-система координат тамагочи Jarvis (только на странице /game). */
export const JARVIS_GAME_GRID_SIZE = 512

/** Стартовая позиция героя — центр мира. */
export const JARVIS_GAME_DEFAULT_POS = { x: 256, y: 256 } as const

/** Одна клетка сетки = один «юнит» Jarvis (визуальный размер аватара = 1 клетка). */
export const JARVIS_GAME_CELL_UNITS = 1
